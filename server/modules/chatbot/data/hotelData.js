// Static chatbot hotel data was intentionally removed.
// The chatbot now reads hotels from server/modules/hotel/hotel.model.js so it
// only recommends records that exist on the website.

module.exports = { hotelData: [] };
